apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: forum-pg-data
  name: forum-pg-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
